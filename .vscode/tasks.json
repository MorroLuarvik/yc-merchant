{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "archive",
            "type": "shell",
            "command": "${workspaceFolder}\\builder\\builder.ps1 ${fileDirname} task.zip c:\\temp",
            "problemMatcher": []
        },
        {
            "label": "execute",
            "type": "shell",
            "command": "${workspaceFolder}\\builder\\executor.ps1 ${fileDirname}",
            "problemMatcher": []
        },
    ]
}